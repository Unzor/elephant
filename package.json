{
  "name": "elephant-runtime",
  "version": "1.0.4",
  "description": "An alternate runtime for Clojure written in JavaScript with a package manager.",
  "main": "index.js",
  "scripts": {
    "postinstall": "echo \"Done installing runtime, installing erpi...\" && npm install -g erpi",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "elephant": "index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.2",
    "cli2json": "^1.0.0"
  }
}
